fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
pydantic-settings==2.5.0
python-multipart==0.0.9

# AI / LLM (Vertex AI SDK - authenticates via service account / ADC)
google-cloud-aiplatform==1.72.0

# ElevenLabs (2.x required for text_to_dialogue API)
elevenlabs>=2.47.0

# PDF Processing
pymupdf4llm>=0.0.17
pymupdf>=1.24.10

# Database (>=2.11 supports httpx>=0.28; 2.9.x pins httpx<0.28)
supabase>=2.11.0,<3.0.0

# HTTP & Async (google-genai requires httpx>=0.28.1)
httpx>=0.28.1,<1.0.0
aiofiles==24.1.0

# Retry logic
tenacity==9.0.0

# Audio processing
pydub==0.25.1

# JWT decoding (Supabase Auth)
python-jose[cryptography]==3.3.0
